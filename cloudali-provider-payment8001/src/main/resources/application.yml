server:
  port: 8001
spring:
  application:
    name: cloudali-provider-payment
  cloud:
    sentinel:
      transport:
        dashboard: localhost:8080
        port: 8719
  profiles:
    active: dev
management:
  endpoints:
    web:
      exposure:
        include: '*'

#spring:
#  profiles:
#    active: dev
#    # active: test
#    # active: pro